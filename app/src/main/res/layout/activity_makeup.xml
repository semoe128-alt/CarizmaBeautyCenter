<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- الصورة الخلفية تغطي كامل الشاشة -->
    <ImageView
        android:id="@+id/fullScreenImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/your_fullscreen_image"
        android:contentDescription="Background Image" />

    <!-- المحتوى فوق الصورة مع تمرير -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#00000000"
        android:paddingHorizontal="24dp"
        android:paddingTop="80dp"
        android:paddingBottom="80dp">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal">

            <!-- عنوان القسم -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="💅 Makeup"
                android:textSize="26sp"
                android:textStyle="bold"
                android:textColor="#E91E63"
                android:layout_marginBottom="32dp"
                android:textAlignment="center" />

            <!-- وصف الخدمة -->
            <TextView
                android:id="@+id/tvMakeupDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="في Carizma Beauty Center نقدم أفضل خدمات المكياج باستخدام أحدث المنتجات وأمهر خبراء التجميل لضمان مظهر متألق وجذاب في كل مناسبة."
                android:textSize="20sp"
                android:lineSpacingExtra="8dp"
                android:textAlignment="center"
                android:textColor="#371020"
                android:layout_marginBottom="40dp" />

        </LinearLayout>
    </ScrollView>

</FrameLayout>
